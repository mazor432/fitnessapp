import React from 'react';
import Navigation from './Navigation/Navigation';


const header = (props) => (

    <header >
        <nav >
            <Navigation />
        </nav>
    </header>
)

export default header;