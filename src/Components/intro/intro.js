import React from 'react';
import style from './intro.sass'
const Intro = () => {
    return (
        <div>
            <h2 className={style.text}>Hello in my application ;] Please sign in to see entirety!</h2>
        </div>
    );
};

export default Intro;